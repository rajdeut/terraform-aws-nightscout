# GCP Configuration
project_id = "your-gcp-project-id"
region     = "us-central1"  # First available zone will be automatically selected

# Domain Configuration (optional)
# domain = "nightscout.yourdomain.com"
# https  = true

# Security Configuration (optional)
# my_ip = "203.0.113.1"  # Your public IP for SSH access restriction

# SSH Key Configuration
compute_ssh_public_key_path = "config/nightscout-compute-key.pub"

# Resource Labels (optional - these are the defaults)
labels = {
  env = "prod"
  app = "nightscout"
}